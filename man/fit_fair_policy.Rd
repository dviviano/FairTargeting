% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_fair_policy}
\alias{fit_fair_policy}
\title{Fit a Fair Policy Targeting rule.}
\usage{
fit_fair_policy(
  data,
  outcome,
  treatment,
  sensitive,
  covariates,
  policy_class = c("maxscore", "probabilistic", "threshold_probabilistic", "tree"),
  distance = NULL,
  fairness = NULL,
  two_directions = TRUE,
  capacity = NULL,
  discretization = NULL,
  n_grid = NULL,
  alpha_seq = NULL,
  lambda = 0,
  solver = "gurobi",
  cross_fit = TRUE,
  n_folds = 5,
  seed = NULL,
  outcome_family = c("auto", "gaussian", "binomial"),
  nuisance = NULL,
  nuisance_method = c("glmnet", "glm"),
  trim = 0.001,
  cost_treatment = 0,
  probabilistic = NULL,
  threshold_probabilistic = NULL,
  no_parity_constraint = NULL,
  noparity_constraint = NULL,
  additional_fairness_constraint = FALSE,
  parity_constraint = ">=",
  UnFairness_bound = Inf,
  max_time = 300,
  max_time_frontier = max_time,
  gurobi_params = NA,
  numcores = 1,
  tolerance_frontier = 10^(-3),
  tolerance_optimization = 10^(-6),
  open_alpha_grid = FALSE,
  tree_num_splits = 4,
  tree_length = 2,
  tree_epsilon_optimality = 0.001,
  ...
)
}
\arguments{
\item{data}{Data frame.}

\item{outcome}{Outcome column name.}

\item{treatment}{Binary treatment column name.}

\item{sensitive}{Binary sensitive-attribute column name. Value 1 denotes the
sensitive group.}

\item{covariates}{Character vector of policy covariates. The policy design
matrix is constructed with the sensitive attribute as the first column,
followed by the listed covariates.}

\item{policy_class}{One of \code{"maxscore"}, \code{"probabilistic"},
\code{"threshold_probabilistic"}, or \code{"tree"}.}

\item{distance}{Fairness criterion. One of \code{"welfare"},
\code{"relative_welfare"}, \code{"parity"}, or \code{"envy"}.}

\item{fairness}{Optional alternative name for \code{distance}.}

\item{two_directions}{Logical. If \code{TRUE}, welfare, relative-welfare, and
parity unfairness are computed as two-sided absolute disparities. If
\code{FALSE}, signed disparities are used.}

\item{capacity}{Optional treatment capacity. Values from 0 to 1 are treated
as fractions; values greater than 1 are treated as counts.}

\item{discretization}{Number of alpha-grid points. Defaults to
\code{floor(sqrt(n))}.}

\item{n_grid}{Optional alternative name for \code{discretization}.}

\item{alpha_seq}{Optional explicit alpha grid. By default, the grid is
\code{seq(0, 1, length.out = discretization)}.}

\item{lambda}{Approximate Pareto-frontier slack parameter.}

\item{solver}{Optimization backend. The default and supported exact backend
is \code{"gurobi"}.}

\item{cross_fit}{Logical. Whether to cross-fit nuisance functions.}

\item{n_folds}{Number of folds for nuisance cross-fitting.}

\item{seed}{Optional random seed.}

\item{outcome_family}{Outcome model family. One of \code{"auto"}, \code{"gaussian"},
or \code{"binomial"}.}

\item{nuisance}{Optional nuisance object returned by \code{estimate_nuisance()}.}

\item{nuisance_method}{Nuisance estimation method. One of \code{"glmnet"} or
\code{"glm"}.}

\item{trim}{Propensity trimming bound.}

\item{cost_treatment}{Optional treatment cost.}

\item{probabilistic}{Optional logical flag for probabilistic policy classes.
If \code{NULL}, it is inferred from \code{policy_class}.}

\item{threshold_probabilistic}{Optional logical flag for
threshold-probabilistic policy classes. If \code{NULL}, it is inferred from
\code{policy_class}.}

\item{no_parity_constraint}{Logical. Whether to exclude the sensitive
attribute from the policy rule while retaining it for group-specific
evaluation.}

\item{noparity_constraint}{Optional alternative name for \code{no_parity_constraint}.}

\item{additional_fairness_constraint}{Logical. Whether to include an
additional fairness constraint in the optimization problem.}

\item{parity_constraint}{Constraint direction for the additional parity
constraint.}

\item{UnFairness_bound}{Optional bound for constrained fairness extensions.}

\item{max_time}{Gurobi time limit for the second-stage fairness optimization.}

\item{max_time_frontier}{Gurobi time limit for each alpha-frontier problem.}

\item{gurobi_params}{Optional Gurobi parameter list.}

\item{numcores}{Number of Gurobi threads.}

\item{tolerance_frontier}{Optimization tolerance for frontier problems.}

\item{tolerance_optimization}{Optimization tolerance for the second-stage
problem.}

\item{open_alpha_grid}{Logical. If \code{TRUE}, use an open alpha grid that
excludes 0 and 1.}

\item{tree_num_splits}{Number of split candidates for tree policies.}

\item{tree_length}{Tree depth parameter.}

\item{tree_epsilon_optimality}{Epsilon-optimality pruning value for tree
search.}

\item{...}{Reserved for future extensions.}
}
\value{
An S3 object of class \code{"fairpolicy"}.
}
\description{
Estimates a Fair Policy Targeting rule using a two-stage optimization
procedure. First, the method estimates a discretized empirical Pareto
frontier over an alpha grid. Second, it solves a fairness optimization problem
subject to approximate Pareto-frontier constraints.
}
\details{
The selected policy is not restricted to be one of the alpha-grid maximizers.
It may be any feasible policy in the model class that satisfies the
approximate Pareto-frontier constraints.
}
